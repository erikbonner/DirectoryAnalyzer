<Window x:Class="DesktopAnalyzer.View.AnalyzerView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DesktopAnalyzer"
        mc:Ignorable="d"
        Title="{Binding ViewTitle}"  SizeToContent="Height" Width="300"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize" WindowStyle="ToolWindow"
        Closed="Window_Closed">
    <Grid>

        <DockPanel Grid.Row="0" >
            <Label DockPanel.Dock="Left">Progress</Label>
            <ProgressBar Value="{Binding ProgressVal}"></ProgressBar>
        </DockPanel>
    </Grid>
</Window>
